\newcommand{\icepick}{Heisenberg's ice-pick}

\begin{question}
  Estimate the rough order of magnitude of the length of time that an
  ice pick can be balanced on its point if the only limitation is that
  set by the Heisenberg uncertainty principle. Assume that the point
  is sharp and that the point and the surface on which it rests are
  hard. You may make approximations which do not alter the general
  order of magnitude of the result. Assume reasonable values for the
  dimensions and weight of the ice pick. Obtain an approximate
  numerical result and express it \emph{in seconds}.
\end{question}

\begin{purpose}
  This problem is \emph{evil}.  There doesn't seem to be a universally
  agreed-upon method of computing it and you'll be forced to take
  several approximations (including modeling the system as two point
  particles connected by an infinitely thin piece of Hyle).  That
  being said, this is kindof a capstone problem.  I'm pretty sure it's
  the hardest problem in the chapter.  It has a little bit of
  everything with a lot of connections to the outside world.  Buckle
  up and enjoy.  Note that I actually solved this problem out of
  order.  Problem \ref{1.36:theQuestion} involves proving one of the
  key relations for the solution to this problem so I decided to solve
  that one first.

  There are several concepts which need to come together for this
  problem:

  \begin{itemize}
  \item Application of the $X/P$ uncertainty relation.
  \item Application of the $E/t$ uncertainty relation.
  \item Taking expectation values using wave functions.
  \item The Wigner function.
  \item Wave function collapse after observations.
  \item Mass energy and momentum implications from special relativity.
  \end{itemize}

  Ultimately, this problem ties together just about all of the
  important topics in chapter 1 and then some.  We're going to solve
  the problem using a few different approaches:
  
  \begin{questionpart}
    The most straight-forward normal way of doing this problem is to
    assume a wave function for the position, determine the \acp{pdf}
    for $P$ and $X$ and integrate against the fall-time as a function
    of those variables.  True to form, things are out of order here so
    we'll have to skip around a bit, starting with working problem
    \ref{1.36:theQuestion}.
  \end{questionpart}

  \begin{questionpart}
    The next fun way to solve the problem is to leverage the $E/t$
    uncertainty relation to determine the amount of rotational kinetic
    energy in the system.  Using the time expended during the
    observation of energy, we then reverse-engineer the presumed
    initial conditions and compute the expecation value just as we did
    in the previous part.
  \end{questionpart}

\end{purpose}

\begin{answer}
  So, if we had a perfect and perfectly-determined world, then the
  ice-pick would be perfectly vertical, made of Hyle, infinitely
  rigid, no mechanical/electrical/use-the-force-luke perturbations and
  it would just sit there until the heat-death of the universe.
  Newsflash to anyone watching Krasnov and global warming...we don't
  live in a perfect world.  The fundamental indeterminability of
  quantum mechanics ensures that we are working with \icepick{}.  That
  means that whatever indeterminability there is results in the
  ice-pick being in a superposition of all of those states.

  If everything is in a superposition of states then it doesn't really
  make sense to ask how long it will take for that specific trial
  since it is unknowable.  What we \emph{can} ask, is what the
  expectation-value is for the fall-time.  But even that statement
  raises questions.  If our infinite set of monkeys with their
  infinite typewriters repeated the experiment an infinite number of
  times would one of them take an infinite amount of time to fall?
  While we're on that topic, what does the ``fall-time'' even mean?

  Taking the last part first and the first part last, ``fall-time'' is
  defined as when a recording-device of some kind recognizes a signal
  which injects all sorts of statistical issues.  When are there
  enough electrons on the gate of the Field Effect Transistors in the
  CMOS logic sufficiently high to get past the ohmic region?  But we
  understand that statistical analysis reasonably-well -- it's a bunch
  of gaussians with $\sigma$'s that we have determined experimentally
  (assuming it happens in time-ranges $\ge 10^{-10}$s).  The more
  interesting quantum mechanics question is about what it means for
  \icepick{} to trigger the sensor.  When \icepick{} falls, its
  gaussian wave-packet is moving and expanding.  Eventually the
  probability that it will have tripped the sensor increases giving us
  a \ac{cdf}.  Since this isn't an electrical engineering class, we
  will instead focus on what happens to the wave function.  Initial
  indeterminability of the momentum (becoming $\omega_0$) and position
  (becoming $\theta_0$) leads to time-expanding indeterminability of
  the position as it falls.  So we can define our ``fall-time'' as the
  time at which the expectation-value of the spatial wave funtion
  (reparameterized for $\theta$) reaches the ground (i.e. $\theta =
  \pi/2$) if we're using a purely quantum approach or when $\theta =
  \pm \pi/2$ if we're using classical mechanics to propagate the ice
  pick.

  Finally, the infinite monkeys...well..they eventually \emph{do}
  cause \icepick{} to balance for an arbitrarily-long time.


  \begin{answerpart}{The ``normal'' way}

    I mean...I \emph{assume} this is the normal way at least.  Like
    the old joke about Werner Heisenberg getting pulled over by the
    cops goes, we can't know our position and momentum at the same
    time.  We just went to great pains to prove that if $\vec{a}^{*}
    \cdot \vec{a} \ge 0$ then the uncertainty relation is a thing, so
    let's use that as our starting point.  However, if we want to
    compute an uncertainty value, then we're going to need to pick a
    wave function to work with and the \ac{gwp} fits the bill for a
    couple of reasons:

    \begin{enumerate}
    \item They are known to minimize the $\hat{X}/\hat{P}$ uncertainty
      (i.e. $\dispersion{\hat{X}}\dispersion{\hat{P}} = \frac{1}{4}
      \abs{\expval{[\hat{X}, \hat{P}]}}^2$

    \item The problem setup actually kinda clues us into the concept
      of using a Gaussian in that it seems quite natural to assume
      that any machine which is capable of placing \icepick{} is
      probably going to have a Gaussian in its placement.
    \end{enumerate}

    Since this is the first problem in which we need to really dig
    into this wave function, we're going to show the full derivation
    below then record the useful results in our \ac{qrf} section
    \ref{q:wavey:gwp}.
    
    \begin{equation}
      \tag{1.146}
      \dispersion{A}\dispersion{B} \ge \frac{1}{4} \abs{\expval{[A, B]}}^2
    \end{equation}

    So swapping out those operators to be the ones we're interested in:
  
    \begin{equation}
      \begin{split}
        \dispersion{x}\dispersion{p}
        &\ge \frac{1}{4} \abs{\expval{[x, p]}}^2\\
        &= \frac{1}{4} \abs{\expval{i\hbar}}^2\\
        &= \frac{\hbar^2}{4}\\
      \end{split}
    \end{equation}

    So, if we have equality, then it means that the only wave
    functions we're going to accept are those which meet that
    equality.  Here, we're going to make an assumption.  Since the
    whole concept is ``place the ice pick and make it reeeeeeally
    steady'' then we're going to assume that placement of the ice-pick
    is a gaussian with respect to the top of the ice pick.  It's also
    worth noting that Gaussian wave packets are known to minimize
    uncertainty.

    \begin{equation}
      \tag{1.267}
      \braket{x'}{\alpha}
      = \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
      \exp[ikx' - \frac{x'^{2}}{2d^2}]
    \end{equation}

    With our spatial wave-function in hand, we now have 4 values to
    compute: $\expval{\hat{X}}$, $\expval{\hat{X}^2}$,
    $\expval{\hat{P}}$, and $\expval{\hat{P}^2}$.  Let's start with
    $\expval{\hat{X}}$.  We're going to use $\psi$ to represent the
    spatial wave-function rather than the more general $\alpha$.

    \begin{equation}
      \begin{split}
        \expval{\hat{X}}
        &= \mel{\psi}{\hat{X}}{\psi}\\
        &= \mel{\psi}{\hat{X}\hat{1}}{\psi}\\
        &= \int dx' (\bra{\psi})(\hat{X}\dyad{x'})(\ket{\psi})\\
        &= \int dx' \mel{\psi}{\hat{X}}{x'}\braket{x'}{\psi}\\
        &= \int dx' x'\braket{\psi}{x'}\braket{x'}{\psi}\\
        &= \int dx' x'\braket{x'}{\psi}^{*}\braket{x'}{\psi}\\
      \end{split}
    \end{equation}

    Bringing in our definition of our spatial wave funtion, we find
    that
    
    \begin{equation}
      \inlabel{exp:x1}
      \begin{split}
        \expval{\hat{X}}
        &= \int dx' x'\braket{x'}{\psi}^{*}\braket{x'}{\psi}\\
        &= \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]^2
        \int dx' x'
        \exp[\cancel{-ikx'} - \frac{x'^{2}}{2d^2}]
        \exp[\cancel{ikx'} - \frac{x'^{2}}{2d^2}]\\
        &= \frac{1}{\sqrt{\pi}d}
        \int dx' x'
        \exp[- \cancel{2}\frac{x'^{2}}{\cancel{2}d^2}]\\
        &= \frac{1}{\sqrt{\pi}d}
        \int dx' x'
        \exp[- \frac{x'^{2}}{d^2}]\\
        &= 0\\
      \end{split}
    \end{equation}

    Using equation \ref{q:integral:xgaussian}, we see that the
    expectation value for position is $0$.  So, this is a long-winded
    way of saying something we already knew to be true: the
    expectation value of a symmetric function centerd about $0$
    is... well... $0$.  You can also shortcut the integral by noting
    that you're integrating an odd function with symmetrical bounds
    which is always $0$ ($\int_{-a}^a dx x \cdot \text{symmetric}(x) =
    0$).  Let's next look at the expectation value of the square.
    We're only going to get a second $x'$ in that integral but let's
    do it anyway for completeness.

    \begin{equation}
      \begin{split}
        \expval{\hat{X}^2}
        &= \expval{\hat{X}\hat{X}}\\
        &= \mel{\psi}{\hat{X}\hat{X}}{\psi}\\
        &= \mel{\psi}{\hat{X}\hat{1}\hat{X}}{\psi}\\
        &= \int dx' (\bra{\psi})(\hat{X}\dyad{x'}\hat{X})(\ket{\psi})\\
        &= \int dx' \mel{\psi}{\hat{X}}{x'}\mel{x'}{\hat{X}}{\psi}\\
        &= \int dx' x'^2\braket{\psi}{x'}\braket{x'}{\psi}\\
        &= \int dx' x'^2\braket{x'}{\psi}^{*}\braket{x'}{\psi}\\
      \end{split}
    \end{equation}

    Once again employing integral-by-table and referencing equation
    \ref{q:integral:xxgaussian}, we find that

    \begin{equation}
      \inlabel{exp:x2}
      \begin{alignedat}{2}
        & &
        \int_{-\infty}^{\infty} dx\ x^2e^{-\alpha x^2}
        &= \frac{1}{2\alpha}\sqrt{\frac{\pi}{\alpha}}\\
        \implies & &
        \frac{1}{\sqrt{\pi}d}
        \int_{-\infty}^{\infty} dx\ x^2\exp(-\frac{x^2}{d^2})
        &= \cancel{\frac{1}{\sqrt{\pi}d}}\frac{d^2}{2}\cancel{\sqrt{d^2\pi}}\\
        &&&= \frac{d^2}{2}\\
      \end{alignedat}
    \end{equation}

    Next up, let's compute the expectation value for the momentum.
    
    \begin{equation}
      \begin{split}
        \expval{\hat{P}}
        &= \mel{\psi}{\hat{P}}{\psi}\\
        &= \mel{\psi}{\hat{1}\hat{P}}{\psi}\\
        &= \int dx' (\bra{\psi})(\dyad{x'}\hat{P})(\ket{\psi})\\
        &= \int dx' \braket{\psi}{x'}\mel{x'}{\hat{P}}{\psi}\\
      \end{split}
    \end{equation}

    Pulling in an identity from the book

    \begin{equation}
      \tag{1.249}
      \mel{x'}{\hat{p}}{\alpha} = -i \hbar \pdv{x'} \braket{x'}{\alpha}
    \end{equation}

    \begin{equation}
      \begin{split}
        \expval{\hat{P}}
        &= \int dx'
        \braket{\psi}{x'}
        \mel{x'}{\hat{P}}{\psi}\\
        &= \int dx'
        \braket{x'}{\psi}^{*}
        \left(-i \hbar \pdv{x'}\right)
        \braket{x'}{\psi}\\
        &= \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]^2
        \int dx'
        \exp[-ikx' - \frac{x'^{2}}{2d^2}]
        \left(-i \hbar \pdv{x'}\right)
        \exp[ikx' - \frac{x'^{2}}{2d^2}]\\
        &= \frac{1}{d\sqrt{\pi}}
        \int dx'
        \exp[-ikx' - \frac{x'^{2}}{2d^2}]
        (-i \hbar)
        \left(
        ik - \frac{\cancel{2}x'}{\cancel{2}d^2}
        \right)
        \exp[ikx' - \frac{x'^{2}}{2d^2}]\\
        &= \frac{\hbar}{d\sqrt{\pi}}
        \int dx'
        \exp[-ikx' - \frac{x'^{2}}{2d^2}]
        \left(
        k + i\frac{x'}{d^2}
        \right)
        \exp[ikx' - \frac{x'^{2}}{2d^2}]\\
        &= \frac{\hbar}{d\sqrt{\pi}}
        \int dx'
        \left(k + i\frac{x'}{d^2}\right)
        \exp[- \frac{x'^{2}}{d^2}]\\
        &= \frac{\hbar}{d\sqrt{\pi}}
        \left\{
        k\int dx'
        \exp[- \frac{x'^{2}}{d^2}]
        + i\int dx'
        \frac{x'}{d^2}\exp[- \frac{x'^{2}}{d^2}]
        \right\}\\
      \end{split}
    \end{equation}

    Once again drawing upon our integrals in the \ac{qrf},
    specifically equations \ref{q:integral:xgaussian} and
    \ref{q:integral:xxgaussian}:

    \begin{equation}
      \inlabel{exp:p1}
      \begin{split}
        \expval{\hat{P}}
        &= \int dx'
        \braket{\psi}{x'}
        \mel{x'}{\hat{P}}{\psi}
        \\
        &= \frac{\hbar}{d\sqrt{\pi}}
        \left\{
        k\int dx'
        \exp[- \frac{x'^{2}}{d^2}]
        + i\int dx'
        \frac{x'}{d^2}\exp[- \frac{x'^{2}}{d^2}]
        \right\}
        \\
        &= \frac{k\hbar}{d\sqrt{\pi}}
        \int dx'
        \exp[- \frac{x'^{2}}{d^2}]
        \\
        &= \frac{k\hbar}{d\sqrt{\pi}}
        \sqrt{d^2\pi}
        \\
        &= k\hbar
        \\
      \end{split}
    \end{equation}
    
    So, that works out quite conveniently doesn't it.  You'll note
    that the $k$ is the imaginary term in the exponential which
    represents the momentum term.  I'm preeeettty sure the gaussian
    wave packet formulation was specifically designed to separate out
    the spatial and momentum contributions.  It also makes it pretty
    easy to see that this wave function has both a spatial and a
    momentum component and thus represents a complete state
    description.  One expectation value left.

    \begin{equation}
      \begin{split}
        \expval{\hat{P}^2}
        &= \mel{\psi}{\hat{P}\hat{P}}{\psi}\\
        &= \mel{\psi}{\hat{P}\hat{1}\hat{P}}{\psi}\\
        &= \int dx' (\bra{\psi})(\hat{P}\dyad{x'}\hat{P})(\ket{\psi})\\
        &= \int dx' \mel{\psi}{\hat{P}}{x'}\mel{x'}{\hat{P}}{\psi}\\
        &= \int dx' \mel{x'}{\hat{P}}{\psi}^{*}\mel{x'}{\hat{P}}{\psi}\\
        &=
        \int dx'
        \left(i \hbar \pdv{x'}\right)
        \braket{x'}{\psi}^{*}
        \left(-i \hbar \pdv{x'}\right)
        \braket{x'}{\psi}
        \\
        &=
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]^2
        \int dx'
        \left\{
        \left(i \hbar \pdv{x'}\right)
        \exp[-ikx' - \frac{x'^{2}}{2d^2}]
        \right\}
        \left\{
        \left(-i \hbar \pdv{x'}\right)
        \exp[ikx' - \frac{x'^{2}}{2d^2}]        
        \right\}
        \\
      \end{split}
    \end{equation}

    Leveraging some intermediate products from before (and being wary
    of sign errors) we find that:

    \begin{equation}
      \begin{split}
        \expval{\hat{P}^2}
        &=
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]^2
        \int dx'
        \left\{
        \left(i \hbar \pdv{x'}\right)
        \exp[-ikx' - \frac{x'^{2}}{2d^2}]
        \right\}
        \left\{
        \left(-i \hbar \pdv{x'}\right)
        \exp[ikx' - \frac{x'^{2}}{2d^2}]        
        \right\}
        \\
        &=
        \frac{1}{d\sqrt{\pi}}
        \int dx'
        (i \hbar)
        \left(
        -ik - \frac{\cancel{2}x'}{\cancel{2}d^2}
        \right)
        \exp[-ikx' - \frac{x'^{2}}{2d^2}]
        (-i \hbar)
        \left(
        ik - \frac{\cancel{2}x'}{\cancel{2}d^2}
        \right)
        \exp[ikx' - \frac{x'^{2}}{2d^2}]
        \\
        &=
        \frac{\hbar^2}{d\sqrt{\pi}}
        \int dx'
        \left(
        k - i\frac{x'}{d^2}
        \right)
        \left(
        k + i\frac{x'}{d^2}
        \right)
        \exp[-\frac{x'^{2}}{d^2}]
        \\
        &=
        \frac{\hbar^2}{d\sqrt{\pi}}
        \int dx'
        \left(
        k^2 + \frac{x'^2}{d^4}
        \right)
        \exp[-\frac{x'^{2}}{d^2}]
        \\
        &=
        \frac{\hbar^2}{d\sqrt{\pi}}
        \left[
        k^2
        \int dx'
        \exp[-\frac{x'^{2}}{d^2}]
        +
        \int dx'
        \frac{x'^2}{d^4}
        \exp[-\frac{x'^{2}}{d^2}]
        \right]
        \\
      \end{split}
    \end{equation}

    Once again bringing out our table of integrals

    \begin{equation}
      \inlabel{exp:p2}
      \begin{split}
        \expval{\hat{P}^2}
        &=
        \frac{\hbar^2}{d\sqrt{\pi}}
        \left[
        k^2
        \int dx'
        \exp[-\frac{x'^{2}}{d^2}]
        +
        \int dx'
        \frac{x'^2}{d^4}
        \exp[-\frac{x'^{2}}{d^2}]
        \right]
        \\
        &=
        \frac{\hbar^2}{d\sqrt{\pi}}
        \left[
        k^2
        d\sqrt{\pi}
        +
        \frac{1}{d^4}
        \frac{d^2}{2}d\sqrt{\pi}
        \right]
        \\
        &=
        \frac{\hbar^2}{\cancel{d\sqrt{\pi}}}
        \cancel{d\sqrt{\pi}}
        \left(
        k^2
        +
        \frac{1}{2d^2}
        \right)
        \\
        &=
        \hbar^2
        \left(
        k^2
        +
        \frac{1}{2d^2}
        \right)
        \\
      \end{split}
    \end{equation}

    \begin{equation}
      \inlabel{exp:combined}
      \begin{alignedat}{2}
        \expval{X}^2 &= 0 & \quad{} &\text{(eq \inref{exp:x1})}\\
        \expval{X^2} &= \frac{d^2}{2} & &\text{(eq \inref{exp:x2})}\\
        \expval{P}^2 &= \hbar^2k^2 & &\text{(eq \inref{exp:p1})}\\
        \expval{P^2} &=
        \hbar^2
        \left(
        k^2
        +
        \frac{1}{2d^2}
        \right)
        & &\text{(eq \inref{exp:p2})}\\
      \end{alignedat}
    \end{equation}
    
    recalling our definition of the variance

    \begin{equation*}
      \tag{1.144}
      \dispersion{A} = \expval{A^2} - \expval{A}^2
    \end{equation*}

    \begin{equation}
      \begin{split}
        \dispersion{X}\dispersion{P}
        &=
        \left(\expval{\hat{X}^2} - \expval{\hat{X}}^2\right)
        \left(\expval{\hat{P}^2} - \expval{\hat{P}}^2\right)
        \\
        &=
        \left(\frac{d^2}{2} - 0\right)
        \left(
        \hbar^2
        \left(
        k^2
        +
        \frac{1}{2d^2}
        \right)
        - \hbar^2k^2
        \right)
        \\
        &=
        \hbar^2
        \frac{\cancel{d^2}}{2}
        \left(
        \cancel{k^2}
        +
        \frac{1}{2\cancel{d^2}}
        - \cancel{k^2}
        \right)
        \\
        &= \frac{\hbar^2}{4}
      \end{split}
    \end{equation}

    Cool...  Well... I guess we just proved that the gaussian wave
    packet does, indeedily-do, minimize the uncertainty.  So, what
    does this tell us?  Well, for starters, it tells us that we made a
    good selection of wave function for our experiment.  Next, it
    gives us some equations to work with that have two variables ($d$
    and $k$) with one representing position indeterminability and the
    other representing group momentum.  As we see above, the group
    velocity doesn't impact the overall indeterminability as it
    cancels out (of course, so does the position indeterminability).
    The $e^{ikx'}$ term is clearly recognizable as both a phasor and
    as an independent operator.  We actually see this operator later
    in problem \ref{1.36:theQuestion} where we call it the Jerk
    Operator.

    This is where we make another simplifying assumption: set $k = 0$.
    Since we're trying our best to keep \icepick{} standing, we're
    going to assume that any momentum is entirely rotational in
    nature.  As such, we can safely set the group momentum to be 0.
    Substituting those values into our expectations from before, we
    find that

    \begin{equation}
      \inlabel{exp:simplified}
      \begin{alignedat}{2}
        \expval{X}^2 &= 0 & \quad{} &\text{(eq \inref{exp:x1})}\\
        \expval{X^2} &= \frac{d^2}{2} & &\text{(eq \inref{exp:x2})}\\
        \expval{P}^2 &= 0 & &\text{(eq \inref{exp:p1} modified)}\\
        \expval{P^2} &= \frac{\hbar^2}{2d^2} & &\text{(eq \inref{exp:p2} modified)}\\
      \end{alignedat}
    \end{equation}
    
    This is where we run into an issue with the concept of an
    expectation-value which is likely to require some limit trickery.
    The denominator in the $\ln$ includes both $\theta_0$ and
    $\omega_0$ which means that it blows up as you approach 0 on both
    of them.  There is one thing that, physically, might help us out
    here -- $P(\omega_0)|_{x_0 \approx 0} \approx 0$.  So let's see if
    that integral converges.  To find out, we'll start by computing
    the probabilities of $x'$ and $p'$.  \S 1.7.3 in the book
    discusses this exact issue and formulates the position and
    momentum space wave functions (which happen to be fourier
    transforms of one another... iiinteresting... kinda makes you
    wonder about Shor's algorithm doesn't it).

    Starting with the statistical interpretation from the text just
    after eq 1.256, ``the probability that a measurement of $p$ gives
    eigenvalue $p'$ within a narrow interval $dp'$ is
    $\abs{\braket{p'}{\alpha}}^2dp'$.  It is customary to call
    $\braket{p'}{\alpha}$ the \textbf{momentum-space wave function};
    the notation $\phi_{\alpha}(p')$ is often used''.  So, if we have
    an expression for $\phi_{\alpha}(p')$ then we can find the
    probability:

    \begin{equation}
      P(p = p' \pm dp'/2) = \abs{\phi_{\alpha}(p')}^2dp'
    \end{equation}

    Later, Sakurai provides us with that exact equation:
    
    \begin{equation}
      \tag{1.266a}
      \psi_{\alpha}(x')
      = \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
      \int dp' \exp(\frac{ip'x'}{\hbar})\phi_{\alpha}(p')
    \end{equation}

    \begin{equation}
      \tag{1.266b}
      \phi_{\alpha}(p')
      = \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
      \int dx' \exp(\frac{-ip'x'}{\hbar})\psi_{\alpha}(x')
    \end{equation}

    Let's use those equations to get ourselves a referenceable
    expression for $\phi(p')$ and $\psi(x')$.  $\psi$ is, of course,
    pretty easy since we started with an ansatz'd sapatial
    wave-function deciding to infer the momentum-space wave-function
    from there.  Going back to 1.267 and re-stating here with our own
    internal numbering that we can more easily reference but also
    inserting our assumption that $k=0$ we find that

    \begin{equation}
      \inlabel{psi}
      \begin{split}
        \psi(x')
        &= \braket{x'}{\alpha}\\
        &= \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
        \exp[-\frac{x'^{2}}{2d^2}]\\
      \end{split}
    \end{equation}
    
    Evaluating $\phi$ we have

    \begin{equation}
      \begin{split}
        \phi(p')
        &= 
        \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
        \int dx' \exp(\frac{-ip'x'}{\hbar})\psi(x')
        \\
        &= 
        \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
        \int dx'
        \exp(\frac{-ip'x'}{\hbar})
        \exp[-\frac{x'^{2}}{2d^2}]
        \\
        &= 
        \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
        \int dx'
        \exp(\frac{-ip'x'}{\hbar}
        - \frac{x'^{2}}{2d^2})
        \\
      \end{split}
    \end{equation}

    Pulling in equation 3.323.2 from our integral table.
    \cite[eq3.323.2]{gr}

    \begin{equation}
      \tag{3.323.2}
      \int_{-\infty}^{\infty} \exp\left(-p^2x^2 \pm qx\right) dx
      = \exp\left(\frac{q^2}{4p^2}\right)\frac{\sqrt{\pi}}{p}
    \end{equation}

    Substituting into our expression above we find that we set $p =
    \frac{1}{d\sqrt{2}}$ and $q = -ip'/\hbar$ to find that


    \begin{equation}
      \inlabel{phi}
      \begin{split}
        \phi(p')
        &= 
        \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
        \int dx' \exp(\frac{-ip'x'}{\hbar})\psi(x')
        \\
        &= 
        \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
        \int dx'
        \exp(\frac{-ip'x'}{\hbar}
        - \frac{x'^{2}}{2d^2})
        \\
        &= 
        \left[ \frac{1}{\sqrt{2\pi\hbar}} \right]
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
        \exp(\frac{q^2}{4p^2})
        \frac{\sqrt{\pi}}{p}
        \\
        &= 
        \left[ \frac{1}{\sqrt{\cancel{2\pi}\hbar}} \right]
        \left[\frac{1}{\pi^{1/4}\sqrt{d}}\right]
        \exp(-\frac{p'^2}{4\hbar^2}2d^2)
        d\cancel{\sqrt{2\pi}}
        \\
        &= 
        \frac{1}{\pi^{1/4}}
        \sqrt{\frac{d}{\hbar}}
        \exp[-\frac{1}{2}\left(\frac{p'd}{\hbar}\right)^2]
        \\
        &= 
        \frac{1}{\pi^{1/4}}
        \sqrt{\frac{d}{\hbar}}
        \exp[-\frac{d^2}{2\hbar^2}p^2]
        \\
      \end{split}
    \end{equation}

    Now that we have expressions for $\psi(x')$ and $\phi(p')$ it's
    worth taking a moment to consider the gaussian nature of them.  A
    ``Normal'' or ``Gaussian'' distribution in classical probability
    theory is expressed as

    \begin{equation}
      \inlabel{normalHog}
      N(\mu, \sigma^2)
      = \frac{1}{\sigma\sqrt{2\pi}}\exp[-\frac{(x-\mu)^2}{2\sigma^2}]
    \end{equation}\incite[inside cover]{hogtan}

    Mapping our equations above, we find that

    \begin{equation}
      \inlabel{p}
      \begin{split}
        P(x')
        &= \abs{\psi(x')}^2
        = \frac{1}{d\sqrt{\pi}} \exp[-\frac{x'^{2}}{d^2}]
        = N(\mu_\psi, \sigma_\psi)
        \\o
        \mu_\psi &= 0\\
        \sigma_\psi &= \frac{d}{\sqrt{2}}\\
        \\
        P(p')
        &= \abs{\phi}^2
        = \frac{1}{\sqrt{\pi}} \frac{d}{\hbar} \exp[-\frac{d^2}{\hbar^2}p^2]
        = N(\mu_\phi, \sigma_\phi)
        \\
        \mu_\phi &= 0\\
        \sigma_\phi &= \frac{\hbar}{d\sqrt{2}}\\
      \end{split}
    \end{equation}

    So, with that parameterization, we see pretty quickly that we have
    an inverse relationship between $\sigma_\psi$ and $\sigma_\phi$
    \ldots want to make one small, ya gotta make the other big.  Armed
    with these expressions, it's time to set up our classical integral
    for the expectation value and see what happens.  Are you as
    excited as I am right now?  No?  Well if you don't like it then
    you can take a hike mister!  Ahheem... moving on.  In a classical
    expectation-value problem for the fall time ($t_f$), we would set
    it up as follows

    \begin{equation}
      \inlabel{theForce}
      \expval{t_f} = \iint_{-\infty}^{\infty} P(x)P(p)t(x, p) dx dp
    \end{equation}

    Here, we have parameterized the fall-time as a function $t(x, p)$
    where the $x$ and $p$ are really $x_0$ and $p_0$ respectively.  We
    already have expressions above for the probabilities so that just
    leaves the classical-mechanics computation of the fall-time as a
    function of initial conditions.  So...that leaves computing the
    fall-time as a function of initial conditions using classical
    mechanics... well... it turns out to be \emph{quite difficult}.
    The thing is, I'm here to do two things... solve quantum mechanics
    problems and chew bubblegum...and I'm all out of minimum-action
    bubblegum.

    I've seen other solutions to this problem which attempt to solve
    Close Encounters of the Third Kind... errm... Elliptic Integrals
    of the First kind with various
    approximations.\incite{tigersPendulum} Instead of spending another
    12 days and 12 pages to solve this on my own, I'm going to just
    let my handy-dandy AI tutor to help me sketch out a solution --
    unlike the rest of the book, I let it handle the strategy and I
    did the verification (so the opposite of normal).  There are going
    to be a few approximations we take which will limit the accuracy
    of our expectation value and I'll do my best to evaluate the order
    of magnitude of the ensuing residual.

    \begin{figure}
      \inlabel{fig:icepick}
      \begin{center}
        \includegraphics[width=2in]{schrodingersIcepick.png}
        \caption{\icepick{}: Note that it consists of two massive
          spheres, each of mass $m$ separated by an infinitely rigid
          inextensible physics-rod of length $L$.  At the time of this
          writing, QCAD doesn't support greek letters so I've used
          $\theta_0 = 0$, $\omega_0 = w$, and $x_0 = x$.}
      \end{center}
    \end{figure}


    Starting with a few basics from figure \inref{fig:icepick}, we see that
    
    \begin{equation}
      \begin{alignedat}{2}
        &&\tau &= I \ddot{\theta} \\
        &&&= mL^2 \ddot{\theta} \\
        &&&= mgL\sin(\theta) \\
        \implies&& 0 &= \ddot{\theta} - \frac{g}{L}\sin(\theta)
      \end{alignedat}
    \end{equation}

    Here is where we make the most important approximation of the
    problem: we're going to use the small angle approximation.  That
    implies that the ice-pick will be more-or-less vertical.  Doing so
    ensures that we can get rid of that $\sin(\theta)$ in favor of
    just $\theta$.  That means that we're assuming we can't maximize
    the fall-time by considering states where $\theta_0 \approx \pi/2$
    and $\omega_0 < 0$ (where negative values for $\omega_0$ indicate
    it's going back towards the normal).  That being said, the problem
    specifies that \icepick{} is to be ``be balanced on its point''.
    So, even if we can increase the fall-time by placing it on its
    edge and kicking it towards the normal, the problem specifies that
    it should be near the top in the region where the small-angle
    approximation holds.  So, if $\expval{\theta_0} = 0$ then we're
    mainly going to care about $P(\theta_0 > 5^\circ)$.  Since we get
    to choose the value of $\sigma_\psi$ ($d$), then we can just limit
    the range of values we consider later to ensure that $P(\theta_0 >
    5^\circ) < 10^{-3}$ or so.  So long as the value doesn't blow up
    to infinity because of the approximation (unlikely) then we should
    easily be within ``the rough order of magnitude'' of the right
    answer.  Employing that approximation, we find that

    \begin{equation}
      \begin{alignedat}{2}
        && 0 &= \ddot{\theta} - \frac{g}{L}\sin(\theta)\\
        \implies{}&& 0 &\approx \ddot{\theta} - \frac{g}{L}\theta\\
      \end{alignedat}
    \end{equation}

    Making a small substitution of $\gamma \equiv \sqrt{g/L}$ we can
    rewrite our ODE and solve it

    \begin{equation}
      \begin{alignedat}{2}
        && 0 &\approx \ddot{\theta} - \frac{g}{L}\theta\\
        &&&= \ddot{\theta} - \gamma^2\theta\\
        \implies{}&& \theta(t) &= A e^{\gamma t} + B e^{-\gamma t}
      \end{alignedat}
    \end{equation}

    As we can see, we essentially have one term with time moving
    forward and one term with time moving backwards, or with one
    exponential growing and one shrinking.  Solving for our constants
    using initial conditions:

    \begin{equation}
      \begin{alignedat}{2}
        && \theta(0) &= A + B = \theta_0\\
        && \dot{\theta}(0) &= \gamma (A - B) = \omega_0\\
        \implies&& A - B &= \frac{\omega_0}{\gamma}\\
        \implies&& (A + B) + (A - B)
        &= \theta_0 + \frac{\omega_0}{\gamma} = 2A\\
        \implies&& A &= \frac{1}{2}\left(\theta_0 + \frac{\omega_0}{\gamma}\right)\\
        \implies&& B &= \frac{1}{2}\left(\theta_0 - \frac{\omega_0}{\gamma}\right)\\
      \end{alignedat}
    \end{equation}

    This is a good place to pause for a moment and consider the
    implications of what the math is telling us.  The two terms there
    almost feel like a reduced mass, or like you have a balance term
    or something like that.  Interestingly, this paradigm seems to
    appear in a number of places including non-linear dynamics,
    good-ole-classmech, and even some quantum problems later on.  For
    now, if the $A$ and $B$ terms balance, then \icepick{} will
    balance forever, otherwise it'll come crashing down eventually.

    So... Skynet is recommending that I disregard the $B$ term, and so
    is the official solutions manual because ``the exponential decay
    will become irrelevant quickly''\incite[p9]{napo}.  That leaves us
    with a final expression for $\theta(t)$

    \begin{equation}
      \inlabel{theta}
      \begin{split}
        \theta(t)
        &= Ae^{\gamma t}\\
        &= \frac{1}{2}\left(\theta_0 + \frac{\omega_0}{\gamma}\right)e^{\gamma t}\\
        &= \frac{1}{2}\left(\theta_0 + \omega_0\sqrt{\frac{L}{g}}\right)\exp[\sqrt{\frac{g}{L}} t]\\
      \end{split}
    \end{equation}

    Now that we have an expression for $\theta$, let's find our
    fall-time.  In a subsequent solution, we're going to examine what
    it means to ``have fallen'' but for now let's just say that it's
    when our solution for $\theta(t_f) = \pm \pi/2$.  The reason we
    have that $\pm$ in there is that it's possible for it to lean
    right and get kicked left (if it leans left and gets kicked-over
    to the right, though, then it's probably an anti-science Krasnov
    supporter and doesn't obey the laws of Quantum Mechanics anyway,
    so we can disregard that case).  If that happens, then

    \begin{equation}
      \begin{split}
        A_+e^{\gamma t_f} &= \frac{\pi}{2}\\
        A_-e^{\gamma t_f} &= -\frac{\pi}{2}\\
        e^{\gamma t_f} &= \frac{\pi}{2\abs{A}}\\
      \end{split}
    \end{equation}

    It seems quite counter-intuitive but for the purposes of computing
    the fall-time we can just take the absolute value of $A$.
    Remembering our approximations for $\theta_0$ and $\omega_0$
    before plugging everything in:

    \begin{equation}
      \begin{split}
        \theta_0 &\approx \frac{x'}{L}\\
        \omega_0 &\approx \frac{p'}{mL}\\
      \end{split}
    \end{equation}
    
    \begin{equation}
      \inlabel{classicaltf}
      \begin{alignedat}{2}
        &&e^{\gamma t_f} &= \frac{\pi}{2\abs{A}}\\
        \implies&&\gamma t_f &= \ln(\frac{\pi}{2\abs{A}})\\
        \implies&&t_f &= \frac{1}{\gamma}\ln(\frac{\pi}{2\abs{A}})\\
        &&&= \sqrt{\frac{L}{g}}\ln(\frac{\pi}{\cancel{2}\abs{\cancel{\frac{1}{2}}\left(\theta_0 + \frac{\omega_0}{\gamma}\right)}})\\
        &&&= \sqrt{\frac{L}{g}}\ln(\frac{\pi}{\abs{\frac{x'}{L} + \frac{p'}{mL}\sqrt{\frac{L}{g}}}})\\
      \end{alignedat}
    \end{equation}

    If we go back and use \inref{theForce} combined with \inref{p}, we
    find that

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &= \iint_{-\infty}^{\infty} P(x')P(p')t(x', p') dx' dp'
        \\
        &= \iint_{-\infty}^{\infty}
        \frac{1}{d\sqrt{\pi}} \exp[-\frac{x'^{2}}{d^2}]
        \frac{1}{\sqrt{\pi}} \frac{d}{\hbar} \exp[-\frac{d^2}{\hbar^2}p'^2]
        \sqrt{\frac{L}{g}}\ln(\frac{\pi}{\abs{\frac{x'}{L} + \frac{p'}{mL}\sqrt{\frac{L}{g}}}})
        dx' dp'
        \\
        &=
        \frac{1}{\pi\hbar}
        \sqrt{\frac{L}{g}}
        \iint_{-\infty}^{\infty}
        \exp[-\frac{x'^{2}}{d^2} - \frac{d^2}{\hbar^2}p'^2]
        \ln(\frac{\pi}{\abs{\frac{x'}{L} + \frac{p'}{mL}\sqrt{\frac{L}{g}}}})
        dx' dp'
        \\
      \end{split}
    \end{equation}

    Well...nobody said it would be pretty. Since we're restricted to a
    great circle anyway and our termination conditions are defined by
    $\theta$ then we might as well switch over to polar coordinates
    and also make a few substitutions to get us to a cleaner integral.

    \begin{equation}
      \begin{alignedat}{2}
        u &= x'\frac{1}{d} &\implies dx' &= d du\\
        v &= p'\frac{d}{\hbar} &\implies dp' &= \frac{\hbar}{d} dv\\
      \end{alignedat}
    \end{equation}

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{1}{\pi\hbar\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-\frac{x'^{2}}{d^2} - \frac{d^2}{\hbar^2}p'^2]
        \ln(\frac{\pi}{\abs{\frac{x'}{L} + \frac{p'}{mL}\sqrt{\frac{L}{g}}}})
        dx' dp'
        \\
        &=
        \frac{1}{\pi\cancel{\hbar}\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\frac{\pi}{\abs{\frac{ud}{L} + \frac{v\hbar}{dmL}\sqrt{\frac{L}{g}}}})
        \cancel{d} du
        \frac{\cancel{\hbar}}{\cancel{d}} dv
        \\
        &=
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\frac{\pi}{\abs{\frac{ud}{L} + \frac{v\hbar}{dmL}\sqrt{\frac{L}{g}}}})
        du dv
        \\
        &=
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\frac{\pi}{\abs{C_1u + C_2v}})
        du dv
        \\
        &=
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\pi)
        du dv
        - 
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\abs{C_1u + C_2v})
        du dv
        \\
      \end{split}
    \end{equation}

    Going back to our table of integrals, the first term is trivial,
    allowing us to collapse our equation before the transition to
    polar coordinates:

    
    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\pi)
        du dv
        - 
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\abs{C_1u + C_2v})
        du dv
        \\
        &=
        \frac{1}{\cancel{\pi}\gamma}
        \ln(\pi)
        \cancel{\sqrt{\pi}\sqrt{\pi}}
        - 
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\abs{C_1u + C_2v})
        du dv
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\abs{C_1u + C_2v})
        du dv
        \\
      \end{split}
    \end{equation}

    OK, we're currently integrating over the $u/v$ plane which is just
    a scaled version of the $x/p$ plane.  I say ``plane'' here, but
    it's less about a geometric plane and more about an abstract plane
    in which all possible values of $(x_0, p_0)$ are represented.
    Switching into polar coordinates within that plane will help with
    the evaluation of the integral itself.  There is no physical
    justification for this step -- it is purely mathematical
    expedience for the purposes of evaluating the integral.  So we're
    going to define $r$ as the radius within our initial-conditions
    space and $\varphi$ as the angle with respect to the positive $x$
    axis.  So mapping back to normal 2D Cartesian that would put
    $+\hat{x}$ along $\hat{x}$.  We're mapping to $u/v$ remember since
    we did our change of variables to make the integral cleaner.

    \begin{equation}
      \begin{split}
        u &= r\cos{\varphi}\\
        v &= r\sin{\varphi}\\
        du dv &= r dr d\varphi
      \end{split}
    \end{equation}

    Substituting those into our earlier expression, we have

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{1}{\pi\gamma}
        \iint_{-\infty}^{\infty}
        \exp[-u^2 - v^2]
        \ln(\abs{C_1u + C_2v})
        du dv
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{1}{\pi\gamma}
        \int_{0}^{\infty}
        \int_{0}^{2\pi}
        \exp[-r^2]
        \ln(\abs{C_1r\cos{\varphi} + C_2r\sin{\varphi}})
        r dr d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{1}{\pi\gamma}
        \int_{0}^{\infty}
        \int_{0}^{2\pi}
        \exp[-r^2]
        \ln(r)
        r dr d\varphi
        -
        \frac{1}{\pi\gamma}
        \int_{0}^{\infty}
        \int_{0}^{2\pi}
        \exp[-r^2]
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        r dr d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{1}{\pi\gamma}
        \int_{0}^{2\pi}
        d\varphi
        \int_{0}^{\infty}
        re^{-r^2}
        \ln(r)
        dr
        -
        \frac{1}{\pi\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        dr
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2}{\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        \ln(r)
        dr
        -
        \frac{1}{\pi\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        dr
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
      \end{split}
    \end{equation}

    Pulling up G\&R, we find

    \begin{equation}
      \tag{3.321.4}
      \int_0^uxe^{-q^2x^2} dx = \frac{1}{2q^2} \left[1 - e^{-q^2u^2}\right]
    \end{equation}\cite[eq 3.321.4]{gr}

    That lets us get rid of one integral

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2}{\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        \ln(r)
        dr
        -
        \frac{1}{\pi\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        dr
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2}{\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        \ln(r)
        dr
        -
        \frac{1}{\pi\gamma}
        \eval\ \frac{1}{2}\left[1 - e^{-r^2}\right]|_{r=0}^{\infty}
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2}{\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        \ln(r)
        dr
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
      \end{split}
    \end{equation}

    Pulling up Wolfram Alpha for that $re^{-r^2}\ln(r)$ integral, we
    find

    \begin{equation}
      \int_{0}^{\infty} re^{-r^2} \ln(r) dr = -\frac{\gamma_e}{4}
    \end{equation}

    where $\gamma_e$ is the Euler-Mascheroni constant.  All told, this
    evaluates to $\approx -0.144304$ which will be called $k_e$.
    Chipping away at our integral, we find that

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2}{\gamma}
        \int_{0}^{\infty}
        re^{-r^2}
        \ln(r)
        dr
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
       -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
      \end{split}
    \end{equation}

    Next up, we use the Harmonic Addition
    Theorem\incite{msu:harmonicAddition} to simplify the $\varphi$ terms.

    \begin{equation}
      \begin{alignedat}{2}
        && f(\theta) &= a\cos(\theta) + b\sin(\theta)\\
        &&&= c\cos(\theta + \delta)\\
        \text{where } && c &= \sqrt{a^2 + b^2}\\
        \text{and } && \delta &= \tan^{-1}(-\frac{a}{b})
      \end{alignedat}
    \end{equation}

    Applying that to our earlier equation, we find that

    \begin{equation}
      \begin{split}
        a &= C_1\\
        b &= C_2\\
        c &= \sqrt{C_1^2 + C_2^2}\\
        \delta &= \tan^{-1}(-\frac{C_1}{C_2})\\
      \end{split}
    \end{equation}

    Substituting into our big nasty expression, we have

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
       -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{C_1\cos{\varphi} + C_2\sin{\varphi}})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{c\cos(\varphi + \delta)})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(c)
        d\varphi
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{\cos(\varphi + \delta)})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
        -
        \frac{\ln(c)}{\gamma}
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{\cos(\varphi + \delta)})
        d\varphi
        \\
      \end{split}
    \end{equation}

    Ladies and Gentlemen, please turn your book of holy scripture to
    equation 4.224.6

    \begin{equation}
      \tag{4.224.6}
      \int_{0}^{\pi/2} \ln(\cos x)dx = -\frac{\pi}{2}\ln(2)
    \end{equation}\cite[eq4.224.6]{gr}

    There are a couple of things to note here.  Because we're
    integrating over a whole period of a periodic function, our phase
    of $\delta$ doesn't actually matter.  However, to get rid of the
    absolute value issue, we're going to integrate over the positive
    portion of the $\cos$ to $\pi/2$ and then multiply by 4.
    Substituting that in, we find that

    \begin{equation}
      \inlabel{theAnswer}
      \begin{split}
        \expval{t_f}
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
        -
        \frac{\ln(c)}{\gamma}
        -
        \frac{1}{2\pi\gamma}
        \int_{0}^{2\pi}
        \ln(\abs{\cos(\varphi + \delta)})
        d\varphi
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
        -
        \frac{\ln(c)}{\gamma}
        -
        \frac{1}{2\pi\gamma}
        4\left(-\frac{\pi}{2}\ln(2)\right)
        \\
        &=
        \frac{\ln(\pi)}{\gamma}
        - 
        \frac{2k_e}{\gamma} 
        -
        \frac{\ln(c)}{\gamma}
        +
        \frac{\ln(2)}{\gamma}
        \\
        &=
        \frac{1}{\gamma}
        \left(
        \ln(2\pi)
        - 
        2k_e
        -
        \ln(c)
        \right)
        \\
      \end{split}
    \end{equation}

    OK...we have our integral for our expectation value for fall-time
    as a function of $d$.  Let's collect a whole crapton of constants
    and see what we're left with.  Of course, the most important thing
    to note at this point is that the answer is, of course, equation
    42.  I'm pretty sure I'm not allowed to go back and add/remove any
    equations now.  Collecting our constants, we have

    \begin{equation}
      \begin{split}
        k_e &\approx -0.144304\\
        C_1 &= \frac{d}{L}\\
        C_2 &= \frac{\hbar}{dmL}\sqrt{\frac{L}{g}}\\
        c &= \sqrt{C_1^2 + C_2^2}\\
        \gamma &= \sqrt{g/L}
      \end{split}
    \end{equation}

    Next up, we plug those constants (or at least the ones that
    contain $d$) back into equation \inref{theAnswer} so we can take a
    derivative with respect to $d$ and find the optimal value for
    maximizing our (classical) fall time.  Since we're taking a
    derivative and only care about those terms which contain $d$,
    we're going to simplify things a bit.

    \begin{equation}
      \begin{split}
        \expval{t_f}
        &=
        \frac{1}{\gamma}
        \left(
        \ln(2\pi)
        - 
        2k_e
        -
        \ln(c)
        \right)
        \\
        &\dot{=}
        -\frac{\ln(c)}{\gamma}
        \\
        &=
        -\frac{1}{\gamma}
        \ln[\sqrt{C_1^2 + C_2^2}]
        \\
        &=
        -\frac{1}{\gamma}
        \ln\left[\sqrt{\frac{d^2}{L^2} + \frac{\hbar^2}{d^2m^2L^2\gamma^2}}\right]
        \\
        &=
        -\frac{1}{\gamma}
        \ln\left[\frac{1}{L}\sqrt{d^2 + \frac{\hbar^2}{d^2m^2\gamma^2}}\right]
        \\
      \end{split}
    \end{equation}

    Since both $\ln$ and $\sqrt{\ }$ are monotonically increasing
    functions, and our objective is to minimize or maximize the value,
    we can focus on minimizing or maximizing the inner portion.  In
    this case, because of that leading negative term, we'll want to
    minimize the inner portion which can be rewritten as

    \begin{equation}
      \begin{alignedat}{2}
        &&0
        &= \left(\pdv{}{d}\right)
        \left(d^2 + \frac{\hbar^2}{d^2m^2\gamma^2}\right)
        \\
        &&&=
        \cancel{2}d - \frac{\cancel{2}\hbar^2}{d^3m^2\gamma^2}
        \\
        &&&=
        1 - \frac{\hbar^2}{d^4m^2\gamma^2}
        \\
        \implies{}&&
        d &=
        \left(
        \frac{\hbar^2}{m^2\gamma^2}
        \right)^\frac{1}{4}
        \\
        &&&=
        \sqrt{
          \frac{\hbar}{m\gamma}
        }
        \\
      \end{alignedat}
    \end{equation}

    Note that you can shortcut the process by leveraging the fact that
    equations of the form $ax^2 + b/x^2$ are minimized when $ax^2 =
    b/x^2$.  Interestingly, that means that when $C_1$ and $C_2$ are
    equal, we're at maximum fall-time.  That means that our best
    strategy is to evenly split our uncertainty budget between our
    position and momentum.

    \begin{figure}[htbp]
      \centering
      \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{by_log_value.pdf}
      \end{minipage}\hfill
      \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{by_linear_ratio.pdf}
      \end{minipage}
      \caption{The log-scale graph on the left clearly illustrates the
        exponential nature of the fall-time as it overcomes initial
        constants.  On the right, we can also see that the ratio of
        the momentum and position uncertainties is also predictive.}
      \inlabel{fig:fallTimeGraphs}
    \end{figure}
    
    Examining figure \inref{fig:fallTimeGraphs}, we can see that zero
    uncertainty in $X$ produces infinite uncertainty in $P$ and the
    fall-time approaches $0$ (at least, it does before you start
    considering relativistic effects anyway).  Alternatively, if you
    let the momentum uncertainty approach $0$, then your starting
    position is probably halfway through the ground to China.  The
    maximum fall-time occurs when (because of the alignment of other
    unit-fixing constants) $d \approx \sqrt{\hbar}$ which works out to
    about $3.9s$.
  \end{answerpart}

  \begin{answerpart}{How Much Time/Energy Do I Have?}
    We're also going to approach the problem from a different
    uncertainty relation: the time/energy uncertainty.  This one is
    outside the scope of chapter 1...it's just fun...

    If we take our observed fall-time (observed in the sense that a
    bell goes ``DING!'' when it falls) then the observed energy
    actually tells us something about the initial momentum.  Given
    that we already have an expression above for the fall time as a
    function of initial position/momentum, then we can use it to
    determine an expression for fall-time as a function of uncertainty
    in the energy.  The selection we're going to make here is how much
    uncertainty do we want in the fall-time vs how much in the initial
    energy (read: momentum).  Then we'll try to find the right balance
    of those uncertainties so that our fall-time is maximized.  We're
    going to ignore uncertainty in position since it'll have a
    negligible impact upon energy.

    We're getting a little bit ahead of ourselves, but in \S2.1.7, we
    find
    
    \begin{equation}
      \tag{2.74}
      \Delta t \Delta E \simeq \hbar
    \end{equation}

    We're actually going to take that one step further with a more
    refined version from Wikipedia using the results of the
    Mandelstam-Tamm theorem resulting in

    \begin{equation}
      \inlabel{etPhoneTime}
      \Delta t \Delta E \ge \frac{\hbar}{2}
    \end{equation}\incite{wikipedia:Uncertainty}

    As mentioned, we're getting a little bit ahead of ourselves, so
    let's just see what happens...maybe we'll be surprised?  It's
    worth remembering that the expression we're using for $\theta(t)$
    is the $Ae^{\gamma t}$ only without the $B$ term, but that should
    be easy enough to add in if necessary.  Let's start by computing
    the classical energy amount which we're going to call $E_0$

    \begin{equation}
      \begin{split}
        E_0 &= T + V\\
        &= \frac{1}{2}I\omega_0^2 + mgL\\
        &= mgL\\
      \end{split}
    \end{equation}

    where $I = mL^2$ and $\omega_0 = 0$ since we're looking at the
    classical energy for a perfectly-placed ice pick.  We're going to
    assume that the energy at impact is a normal variate.  While
    \emph{technically} the energy \emph{isn't} a normal variate since
    it can't be less than $0$, we're also working with quantum
    mandated uncertainties on the order of $10^{-17}$ as compared with
    macroscopic energy levels so... I'm gonna allow it.  So, generally
    speaking, what we're looking for is something form

    \begin{equation}
      t_f = f(\omega_0) = f(\omega_0(E))
    \end{equation}

    We already have fall-time as a function of initial momentum, so
    now we just need to know what that initial momentum is.  We can
    determine that by looking at a new energy value which we're going
    to call the kick ($K$)

    \begin{equation}
      K \equiv \frac{1}{2}I\omega_0^2
    \end{equation}

    We know that the energy at impact is being modeled as a normal
    variate, which means we can express it as follows

    \begin{equation}
      E = N(E_0, \sigma_E)
    \end{equation}

    So, now we can define our full equation for K

    \begin{equation}
      K = N(E_0, \sigma_E) - E_0 = N(0, \sigma_E)
    \end{equation}

    To avoid naming collisions, we're going to go back to calling that
    $E$.  With a value for the value formerly known as $K$ (now known
    as $E$) in hand, we can then express our final fall-time as

    \begin{equation}
      t_f = f(E=N(0, \sigma_E))
    \end{equation}

    Ultimately, we're still looking for an expectation value which
    will be of the form

    \begin{equation}
      \expval{t_f} = \int P(E)t(E) dE
    \end{equation}

    Interestingly, we aren't really using the $\hbar/2$ limit.  Let's
    see if that emerges.  Referencing equation \inref{classicaltf} and
    keeping it in $\omega_0$ form

    \begin{equation}
      \begin{alignedat}{2}
        &&\expval{t_f}
        &= \int P(E)t(E) dE\\
        &&&=
        \int_{-\infty}^{\infty}
        dE
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \sqrt{\frac{L}{g}}\ln(\frac{\pi}{\abs{
        \left(\theta_0 + \frac{\omega_0}{\gamma}\right)}})
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        \int_{-\infty}^{\infty}
        dE
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \ln(\frac{\pi}{\abs{\frac{\omega_0}{\gamma}}})
        \\
      \end{alignedat}
    \end{equation}

    Next, we'll draw upon our energy relationship that $E = 1/2 I
    \omega^2$ and also change our bounds of integration from $-\infty$
    to $0$ and we'll multiply by 2.  That should allow us to eliminate
    the absolute value to make our integral easier.

    \begin{equation}
      \begin{alignedat}{2}
        &&\expval{t_f}
        &= \int P(E)t(E) dE\\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        \int_{-\infty}^{\infty}
        dE
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \ln(\frac{\pi}{\abs{\frac{\omega_0}{\gamma}}})
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \int_{0}^{\infty}
        dE
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \ln(\frac{\gamma\pi}{\omega_0})
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \int_{0}^{\infty}
        dE
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \ln(\frac{\gamma\pi}{\sqrt{\frac{2E}{mL^2}}})
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \int_{0}^{\infty}
        dE
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \ln(\frac{\gamma \pi L \sqrt{m}}{\sqrt{2E}})
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \int_{0}^{\infty}
        dE
        \exp\left[-\frac{E^2}{2\sigma_E^2}\right]
        \ln(k_tE^{-1/2})
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \int_{0}^{\infty}
        dE
        e^{-k_kE^2}
        +
        \int_{0}^{\infty}
        dE
        e^{-k_kE^2}
        \ln(E^{-1/2})
        \right\}
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        +
        \int_{0}^{\infty}
        dE
        e^{-k_kE^2}
        \ln(E^{-1/2})
        \right\}
        \\
      \end{alignedat}
    \end{equation}

    Let's start breaking things apart to get into position to finalize
    our integral using Wolfram Alpha once again

    \begin{equation}
      \begin{alignedat}{2}
        &&\expval{t_f}
        &= \int P(E)t(E) dE\\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        +
        \int_{0}^{\infty}
        dE
        e^{-k_kE^2}
        \ln(E^{-1/2})
        \right\}
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        -
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \int_{0}^{\infty}
        du
        e^{-u^2}
        \ln(\frac{u}{\sqrt{k_k}})
        \right\}
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        -
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \left[
        \int_{0}^{\infty}
        du
        e^{-u^2}
        \ln(u)
        -
        \ln(\sqrt{k_k})
        \int_{0}^{\infty}
        du
        e^{-u^2}
        \right]
        \right\}
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        -
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \left[
        \int_{0}^{\infty}
        du
        e^{-u^2}
        \ln(u)
        -
        \ln(\sqrt{k_k})
        \frac{\sqrt{\pi}}{2}
        \right]
        \right\}
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        -
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \left[
          \left(
          -\frac{\sqrt{\pi}}{4}(\gamma_e + 2\ln(2))
          \right)
        -
        \ln(\sqrt{k_k})
        \frac{\sqrt{\pi}}{2}
        \right]
        \right\}
        \\
      \end{alignedat}
    \end{equation}

    Expanding the giant mess to be even bigger and messier

    \begin{equation}
      \begin{alignedat}{2}
        &&\expval{t_f}
        &= \int P(E)t(E) dE\\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        -
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \left[
          \left(
          -\frac{\sqrt{\pi}}{4}(\gamma_e + 2\ln(2))
          \right)
        -
        \ln(\sqrt{k_k})
        \frac{\sqrt{\pi}}{2}
        \right]
        \right\}
        \\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(k_t)
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        +
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \frac{\sqrt{\pi}}{4}
        \gamma_e
        +
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \frac{\sqrt{\pi}}{4}
        2\ln(2)
        +
        \frac{1}{2}
        \frac{1}{\sqrt{k_k}}
        \ln(\sqrt{k_k})
        \frac{\sqrt{\pi}}{2}
        \right\}
        \\
      \end{alignedat}
    \end{equation}


    Collecting our constants

    \begin{equation}
      \begin{split}
        k_k &= \frac{1}{2\sigma_E^2}\\
        k_t &= \gamma \pi L \sqrt{\frac{m}{2}}\\
      \end{split}
    \end{equation}

    Aaaand plugging them in
    
    \begin{equation}
      \begin{alignedat}{2}
        &&\expval{t_f}
        &= \int P(E)t(E) dE\\
        &&&=
        \frac{1}{\sigma_E\sqrt{2\pi}}
        \frac{1}{\gamma}
        2
        \left\{
        \ln(\gamma \pi L \sqrt{\frac{m}{2}})
        \frac{1}{2}\sigma_E\sqrt{2\pi}
        +
        \frac{1}{2}
        \frac{1}{\sqrt{\frac{1}{2\sigma_E^2}}}
        \frac{\sqrt{\pi}}{4}
        \gamma_e
        +
        \frac{1}{2}
        \frac{1}{\sqrt{\frac{1}{2\sigma_E^2}}}
        \frac{\sqrt{\pi}}{4}
        2\ln(2)
        +
        \frac{1}{2}
        \frac{1}{\sqrt{\frac{1}{2\sigma_E^2}}}
        \ln(\sqrt{\frac{1}{2\sigma_E^2}})
        \frac{\sqrt{\pi}}{2}
        \right\}
        \\
        &&&=
        \frac{1}{\gamma}
        \ln(\gamma \pi L \sqrt{\frac{m}{2}})
        +
        \frac{\gamma_e}{4\gamma}
        +
        \frac{\ln(2)}{2\gamma}
        +
        \frac{1}{2\gamma}
        \ln(\sqrt{\frac{1}{2\sigma_E^2}})
        \\
        &&&=
        \frac{1}{\gamma}
        \left(
        \ln(\gamma \pi L \sqrt{\frac{m}{2}})
        +
        \frac{\gamma_e}{4}
        +
        \frac{\ln(2)}{2}
        +
        \frac{1}{2}
        \ln(\sqrt{\frac{1}{2\sigma_E^2}})
        \right)
        \\
        &&&=
        \frac{1}{\gamma}
        \left(
        \ln(\gamma \pi L \sqrt{\frac{m}{2}})
        +
        \frac{\gamma_e}{4}
        +
        \frac{\ln(2)}{2}
        -
        \frac{1}{4}
        \ln(2\sigma_E^2)
        \right)
        \\
        &&&=
        \frac{1}{\gamma}
        \left(
        \ln(\gamma \pi L \sqrt{\frac{m}{2}})
        +
        \frac{\gamma_e}{4}
        +
        \frac{\ln(2)}{2}
        -
        \frac{1}{4}
        (2\ln(\sigma_E) + \ln(2))
        \right)
        \\
        &&&=
        \frac{1}{\gamma}
        \left(
        \ln(\gamma \pi L \sqrt{\frac{m}{2}})
        +
        \frac{\gamma_e}{4}
        +
        \frac{\ln(2)}{4}
        -
        \frac{\ln(\sigma_E)}{2}
        \right)
        \\
      \end{alignedat}
    \end{equation}

    This leaves us with a final closed-form solution for the
    expectation value of fall-time as a function of the energy
    uncertainty

    \begin{equation}
      \inlabel{exp:tfe}
      \expval{t_f}
      =
      \frac{1}{\gamma}
      \left(
      \ln(\gamma \pi L \sqrt{\frac{m}{2}})
      +
      \frac{\gamma_e}{4}
      +
      \frac{\ln(2)}{4}
      -
      \frac{\ln(\sigma_E)}{2}
      \right)
    \end{equation}
    
    
    Now that we have a closed-form solution for $\expval{t_f}$ let's
    take a look back at that missing uncertainty relation and what it
    means to us.  You'll probabl note that it's monotonic and the less
    uncertainty in energy you have the higher the fall-time ad
    infinitum.  So, clearly that can't be right.  Remember earlier
    when we remarked upon how odd it was that we weren't using the
    uncertainty relation?  Guess what we're going to do now.

    So if we want to maximize our fall-time then we'll want to
    minimize our $\sigma_E$.  To minimize $\sigma_E$, we need to
    constrain it to what is permissible by the uncertainty relation.
    If we use the statistical interpretation of $\Delta t$ (as opposed
    to time-until-change or time for an expectation-value of an
    observable to update by $1\sigma$ or ...)  then we can use our
    uncertainty relation to show that

    \begin{equation}
      \sigma_t = \frac{\hbar}{2\sigma_E}
    \end{equation}

    This allows us to choose a $\sigma_E$ that is as small as possible
    before we violate the $\Delta E \Delta t$ uncertainty principle.
    Looking back at our equation for the fall time we see that we can
    rewrite it to simplify it a bit

    \begin{equation}
      \inlabel{tfe}
      \begin{split}
        t_f
        &=
        \frac{1}{\gamma}
        \ln(\abs{\frac{\gamma\pi}{\omega_0}})
        \\
        &=
        \frac{\ln(\gamma\pi)}{\gamma}
        -
        \frac{\ln(\abs{\omega_0})}{\gamma}
        \\
        &=
        \frac{\ln(\gamma\pi)}{\gamma}
        -
        \frac{1}{\gamma}
        \ln(\frac{\sqrt(2E)}{L\sqrt(m)})
        \\
        &=
        \frac{\ln(\gamma\pi)}{\gamma}
        -
        \frac{1}{2\gamma}
        \ln(\frac{2}{L^2m})
        -
        \frac{\ln(E)}{2\gamma}
        \\
        &=
        C_t
        -
        \frac{\ln(E)}{2\gamma}
        \\
      \end{split}
    \end{equation}

    The variance of $f(x) + C$ is the same as that of $f(x)$ because a
    constant offset doesn't change how spikey/spready the function is.
    So we can disregard $C_t$.  That just leaves us with

    \begin{equation}
      \begin{alignedat}{2}
        &&Var(t_f)
        &= Var(C_t - \frac{\ln(E)}{2\gamma})\\
        &&&= Var(\frac{\ln(E)}{2\gamma})\\
        &&&= \frac{Var(\ln(E))}{4\gamma^2}\\
        &&&= \frac{1}{4\gamma^2}\frac{\pi^2}{8}\\
        \implies &&\sigma_t
        &= \sqrt{\frac{1}{4\gamma^2}\frac{\pi^2}{8}}\\
        &&&= \frac{\pi}{4\sqrt{2}\gamma}\\
      \end{alignedat}
    \end{equation}

    Plugging that value of $\sigma_t$ into our uncertainty relation
    gives us $\sigma_E = \hbar/\sigma_t$.  Plugging that into our
    fall-time equation gives us...drumroll please... $\approx 3.8s$.

    So yeah... science... it works!  We successfully solved the
    expectation-value of the fall-time for \icepick{} using the
    time/energy uncertainty relation.  This calls for scotch!
  \end{answerpart}

\end{answer}
